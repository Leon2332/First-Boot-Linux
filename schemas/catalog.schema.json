{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://firstboot.local/schemas/catalog.schema.json",
  "title": "First Boot Linux shop catalog",
  "description": "Per-shop catalog.json on the USB payload. Self-contained; the chooser does not read official-catalog.json.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "recommended", "catalog"],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1
    },
    "recommended": {
      "type": "array",
      "description": "Chooser grid. Editions may be local or download-only. Non-redistributable official rows (e.g. MS Windows) must not have local editions.",
      "items": { "$ref": "#/$defs/distro" }
    },
    "catalog": {
      "type": "array",
      "description": "Download-only extras. Ids must not also appear in recommended.",
      "items": { "$ref": "#/$defs/distro" }
    }
  },
  "$defs": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
    },
    "sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "distro": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "name",
        "version",
        "tagline",
        "description",
        "family",
        "install",
        "editions"
      ],
      "properties": {
        "id": { "$ref": "#/$defs/id" },
        "name": { "type": "string", "minLength": 1 },
        "version": { "type": "string", "minLength": 1 },
        "tagline": { "type": "string", "minLength": 1 },
        "description": { "type": "string", "minLength": 1 },
        "family": {
          "type": "string",
          "enum": ["ubuntu", "mint", "fedora", "debian", "suse", "windows", "bsd", "other"]
        },
        "install": {
          "$ref": "#/$defs/id",
          "description": "Baked-in driver id (ubuntu-2604, mint-223, fedora-44-plasma, or an old alias), a reserved id with no driver yet (windows, freebsd, debian-preseed), or a shop pack folder payload/custom/<install>/."
        },
        "secure_boot": {
          "type": "boolean",
          "description": "Optional. Default true when omitted (old sticks). False: not signed for Secure Boot."
        },
        "editions": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/edition" }
        }
      }
    },
    "edition": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "name",
        "default",
        "local",
        "sha256",
        "size_bytes"
      ],
      "properties": {
        "id": { "$ref": "#/$defs/id" },
        "name": { "type": "string", "minLength": 1 },
        "default": { "type": "boolean" },
        "local": { "type": "boolean" },
        "file": {
          "type": "string",
          "pattern": "^images/[^/\\\\]+\\.(iso|img)$",
          "description": "Required when local is true. Relative to the payload root."
        },
        "url": {
          "type": "string",
          "format": "uri",
          "description": "Required when local is false."
        },
        "sha256": { "$ref": "#/$defs/sha256" },
        "size_bytes": {
          "type": "integer",
          "minimum": 1
        }
      },
      "allOf": [
        {
          "if": {
            "properties": { "local": { "const": true } },
            "required": ["local"]
          },
          "then": { "required": ["file"] }
        },
        {
          "if": {
            "properties": { "local": { "const": false } },
            "required": ["local"]
          },
          "then": { "required": ["url"] }
        }
      ]
    }
  }
}
