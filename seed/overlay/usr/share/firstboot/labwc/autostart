#!/bin/sh
# labwc waits for this script to exit before it processes clients.
for cmd in pipewire pipewire-pulse wireplumber; do
  if command -v "$cmd" >/dev/null 2>&1; then
    nohup "$cmd" >/dev/null 2>&1 &
  fi
done
exit 0
